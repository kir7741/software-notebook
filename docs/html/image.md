---
sidebar_position: 1
tags:
  - HTML
  - image
last_update:
  date: 2024/06/23
  author: Joseph Lin
---

# Image

要在 HTML 中使用圖片很簡單，只需要使用 `<img>` 標籤就可以了。

```html
<img src="image.jpg" alt="image">
```

`src` 屬性用來指定圖片的路徑，`alt` 則用來指定圖片的替代文字。

#### 圖片種類
